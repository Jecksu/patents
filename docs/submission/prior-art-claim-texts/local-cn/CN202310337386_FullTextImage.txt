                 (19)国家知识产权局

                                                   (12)发明专利申请
                                                                (10)申请公布号 CN 116433837 A
                                                                (43)申请公布日 2023.07.14
                 (21)申请号 202310337386 .4

                 (22)申请日 2023 .03 .31

                 (71)申请人 浙江大学
                     地址 310058 浙江省杭州市西湖区余杭塘
                        路866号

                 (72)发明人 程锦 吕昊               胡伟飞   刘振宇
                         谭建荣

                 (74)专利代理机构 杭州求是专利事务所有限公
                            司 33200
                     专利代理师 刘静
                 (51)Int .Cl .
                     G06T 17/00 (2006 .01)
                     G06F 30/12 (2020 .01)
                     G06V 10/74 (2022 .01)

                                                          权利要求书3页    说明书8页   附图3页

                 (54)发明名称
                     一种基于关键点匹配的三维CAD模型差异分
                 析方法
                 (57)摘要
                     本发明公开了一种基于关键点匹配的三维
                 CAD模型差异分析方法，该方法首先将三维CAD模
                 型转化为关键点表示，  然后引入图表示和图匹配
                 方法建立两模型间顶点的预匹配，  并通过误匹配
                 消除方法得到两模型间的刚体变换参数，   之后利
                 用刚体变换检验建立关键点的匹配关系，   得到关
                 键点匹配序列后进行逐级比较，  从而识别顶点、
                 边、 面和实体模型的差异，能够快速、 准确识别三
                 维CAD模型间的差异。
CN 116433837 A
CN 116433837 A           权   利   要   求   书               1/3 页

   1 .一种基于关键点匹配的三维CAD模型差异分析方法，  其特征在于，包括如下步骤：
    S1： 将待比较的两个三维CAD模型转化为STP格式；
    S2： 从三维CAD模型中提取顶点、边控制点、面控制点三类关键点；
   从模型中提取边的交点作为顶点，     并记录顶点的三维坐标；
    将模型的边转化为非均匀有理B样条曲线NURBS形式，  从各边中提取的除顶点外的所有
NURBS控制点作为模型的边控制点，   并记录其三维坐标和权重；
    将模型的面转化为NURBS形式， 从各面中提取的除顶点外的所有NURBS控制点作为模型
的面控制点，     并记录其三维坐标和权重；
    S3： 将三维CAD模型转化为图表达形式以进行顶点预匹配， 模型的每个面对应图的一个
节点，   若模型中的两个面共边，  则这两个面在图中对应的两个节点间存在一个连接边，  对应
两面的公共边，     否则两个节点间不存在连接边；
    S4： 计算两图节点间和连接边间的相似度，  包括如下子步骤：
         在节点对应的模型面上利用NURBS公式进行均匀采样，
    S4 .1：                         即：




    其中                   n＝1 ,...,N1×N1 ，
            为第i个节点Gi的采样点集，                    Si (u ,v)为节点Gi对
                                        N1为采样数，
应的模型面Si上利用NURBS公式得到的采样点；
                            即：
  在连接边对应的模型边上利用NURBS公式进行均匀采样，




    其中                           m＝1 ,...,N2 ，
            为第i ,j个节点间连接边Gij的采样点集，                 Cij (u)为连
                                             N2为采样数，
接边Gij对应的模型边Cij上利用NURBS公式得到的采样点；
         基于刚体变换计算两点集的平均匹配距离误差，
    S4 .2：                     对任意点数相同的两点集{pk}和
{qk}，寻找最优刚体变换：



    其中K为点集中的点数，    R * 和T * 为最优旋转矩阵和最优平移矩阵 ，
                                           p k ,q k 分别为两点集
{pk} ,{qk}中第k对点的三维坐标，   解得：




    其中                    U和V是协方差矩阵S c 的奇异值分解 ，协方差矩阵S c ＝



                                 2
CN 116433837 A             权   利    要   求   书                  2/3 页

XYT ，
    其中X ,Y分别由列向量               和          k＝1 ,2 ,...,K；
                                        组成，
                    计算平均匹配距离误差为：
    求得最优旋转矩阵和最优平移矩阵后，



         基于两点集的平均匹配距离误差，
    S4 .3：                      分别计算节点间相似度和连接边间相似度；
    两图G ,G '中的两节点Gi ,G 'i '的相似度为：



    其中      为图G中节点Gi的采样点集，          为图G '中节点G 'i '的采样点集；
    两图G ,G '中的两连接边Gij ,G 'i 'j '的相似度为：


    其中      为图G中连接边Gij的采样点集，            为图G '中连接边G 'i 'j '的采样点集；
     S5：利用图匹配方法对两模型的顶点进行预匹配，                       其元素
                                   定义两图G ,G '的匹配矩阵M，
                                否则为0；
mii '在图G的节点Gi和图G '的节点G 'i '匹配时为1，   寻找使总相似度最大的最优匹配
      *
矩阵M ：




    使用投影不动点迭代法求解M* ，              得到两图的节点匹配结果， 图中匹配的两节点在模型中
对应的面即为预匹配的面，              当两模型中的两边所邻接的面均为预匹配的面时，       这两条边为
预匹配的边，        同样地，   当两模型中的两顶点所邻接的边均为预匹配的边时，        这两个顶点为预
匹配的顶点；
    S6： 通过误匹配消除方法计算模型间刚体变换关系，                         计算预匹配
                                             给定最大允许误差ε，
顶点集的平均匹配距离误差，              若其小于最大允许误差ε，     则此时的最优旋转矩阵和最优平移
                                M  M
矩阵即为模型间的刚体变换参数R 和T ；                    分别计算每对预匹配顶点的匹配距离误差：
                                      否则，
                        *     *
    Ek ({pk} ,{qk})＝||(R ·pk+T )‑qk||
    其中Ek ({pk} ,{qk})为点集{pk} ,{qk}中第k对预匹配顶点的匹配距离误差，   pk ,qk为点集
{pk} ,{qk}中第k对预匹配顶点的三维坐标；
    在预匹配顶点集中剔除匹配距离误差最大的顶点对后重新计算其平均匹配距离误差
并与最大允许误差ε进行比对，              直至求出模型间的刚体变换参数RM和TM ；
    S7： 匹配顶点、边控制点和面控制点三类关键点，               将所求得的刚体变换参数RM 和TM代入
两模型的每两个顶点，            计算匹配误差：



    其中           为两模型顶点集   和                      若求得的匹配误差
                                   第i ,i '个顶点的三维坐标；
小于最大允许误差ε，  则对应的两顶点为匹配的顶点；
    M  M
  将R 和T 代入每对边控制点，并分别计算每对边控制点的匹配误差：




                                    3
CN 116433837 A          权    利    要    求   书              3/3 页

    其中           为两模型边控制点集    和                        w为相应控制
                                      第i ,i '个边控制点的三维坐标，
     若求得的匹配误差
点的权重值；                            则对应的两边控制点为匹配的边控
                         小于最大允许误差ε，
制点；
  将RM和TM代入每对面控制点，
                并分别计算每对面控制点的匹配误差：



    其中           为两模型面控制点集    和                         w为相应控
                                       第i ,i '个面控制点的三维坐标，
      若求得的匹配误差
制点的权重值；                               则对应的两面控制点为匹配的面
                             小于最大允许误差ε，
控制点；
  S8：              对两模型的每两条边，
      逐层判断三维CAD模型差异，         若其包含的顶点和边控制点均
一一匹配，  则这两条边视为相同边， 否则视为差异边；对两模型中每两个面，若其包含的面
控制点均一一匹配，   且其组成边均为相同边，则这两个面视为相同面，否则视为差异面；若
两模型的所有面均为相同面，   则两模型为相同的模型，否则两模型间存在差异。
  2 .根据权利要求1所述的一种基于关键点匹配的三维CAD模型差异分析方法，其特征在
 所述S2中，
于，       边控制点的获取方法具体为：
                   对边所在的曲线
    将模型的边转化为NURBS形式，                       有：




           n为B样条曲线的控制点个数，
    其中u为自变量，                      Ni ,k (u)为第i个k
                        k为B样条曲线的阶次，
                     wi为第i个控制点对应的权重；
阶B样条基函数， 为第i个控制点的三维坐标，             从各边中提
                          并记录其三维坐标和权重。
取的除顶点外的所有NURBS控制点作为模型的边控制点，
                                      其特征在
  3 .根据权利要求1所述的一种基于关键点匹配的三维CAD模型差异分析方法，
 所述S2中，
于，      面控制点的获取方法具体为：
                   对面所在的曲面
    将模型的面转化为NURBS形式，                           有：




              n ,m为B样条曲线两个维度的控制点个数，
    其中u ,v为自变量，                   k ,l为B样条曲线两个维度
   Ni ,k (u)为第i个k阶B样条基函数，
的阶次，                    Nj ,l (v)为第j个l阶B样条基函数， 为在位置(i ,
j)处的控制点的三维坐标，  wi ,j为在位置(i ,j)处的控制点对应的权重； 从各面中提取的除
顶点外的所有NURBS控制点作为模型的面控制点，       并记录其三维坐标和权重。
   4 .根据权利要求1所述的一种基于关键点匹配的三维CAD模型差异分析方法，        其特征在
于，在得到三维CAD模型差异分析结果后，    采用Opencascade平台搭建三维CAD模型差异分析
展示界面，   显示待比对的两个三维CAD模型，   并将其中所有的差异面高亮显示。



                                  4
CN 116433837 A           说   明   书                   1/8 页


                 一种基于关键点匹配的三维CAD模型差异分析方法

技术领域
[0001] 本发明涉及三维CAD模型差异分析领域，
                         尤其涉及一种基于关键点匹配的三维CAD
模型差异分析方法。

背景技术
[0002] CAD技术在数字化设计和智能生产中有着广泛应用，在产品从初始版本到最终版
本的迭代中需要对三维CAD模型进行不断的修改和优化，  准确快速地识别不同版本之间CAD
模型的变化对保证生产效率和产品质量至关重要。      三维CAD模型的差异识别技术在模
                          同时，
型检索与重用、产品信息管理、   产品数据交换中也有着广泛应用。然而，现有的CAD模型差异
分析技术要求模型使用同一种建模方式创建，    且无法识别处于不同空间位置的模型，  不具
有通用性。

发明内容
[0003] 本发明的目的在于针对现有的三维CAD模型的差异识别技术通用性、           鲁棒性不强
的问题，   提供一种基于关键点匹配的三维CAD模型差异分析方法，     该方法首先将三维CAD模
型转化为关键点表示，       然后引入图表示和图匹配方法建立两模型间顶点的预匹配，         并通过
误匹配消除方法得到两模型间的刚体变换参数，        之后利用刚体变换检验建立关键点的匹配
关系，   得到关键点匹配序列后进行逐级比较，    从而识别边、面和实体模型的差异，       能够快速、
准确识别三维CAD模型间的差异。
[0004] 本发明的目的是通过以下技术方案来实现的：     一种基于关键点匹配的三维CAD模
型差异分析方法，      该方法包括以下步骤：
[0005] S1： 将待比较的两个三维CAD模型转化为STP格式；
[0006] S2： 从三维CAD模型中提取顶点、边控制点、面控制点三类关键点；
[0007] 从模型中提取边的交点作为顶点，    并记录顶点的三维坐标；
[0008] 将模型的边转化为非均匀有理B样条曲线(NURBS，   Non‑Uniform Rational B‑
Spline)形式，  从各边中提取的除顶点外的所有NURBS控制点作为模型的边控制点，        并记录
其三维坐标和权重；
[0009] 将模型的面转化为NURBS形式， 从各面中提取的除顶点外的所有NURBS控制点作为
模型的面控制点，      并记录其三维坐标和权重；
[0010] S3： 将三维CAD模型转化为图表达形式以进行顶点预匹配，    模型的每个面对应图的
一个节点，   若模型中的两个面共边，   则这两个面在图中对应的两个节点间存在一个连接边，
对应两面的公共边，      否则两个节点间不存在连接边；
[0011] S4： 计算两图节点间和连接边间的相似度，  包括如下子步骤：
[0012] S4 .1：
            在节点对应的模型面上利用NURBS公式进行均匀采样，   即：

[0013]



                             5
CN 116433837 A                说    明   书                        2/8 页

[0014]   其中                   n＝1 ,...,N1×N1 ，
                 为第i个节点Gi的采样点集，                    Si (u ,v)为节点
                                             N1为采样数，
Gi对应的模型面Si上利用NURBS公式得到的采样点；
[0015] 在连接边对应的模型边上利用NURBS公式进行均匀采样，
                                 即：

[0016]


[0017]   其中                           m＝1 ,...,N2 ，
                 为第i ,j个节点间连接边Gij的采样点集，                 Cij (u)
                                                  N2为采样数，
为连接边Gij对应的模型边Cij上利用NURBS公式得到的采样点；
[0018] S4 .2：
            基于刚体变换计算两点集的平均匹配距离误差， 对任意点数相同的两点集
{pk}和{qk}，寻找最优刚体变换：

[0019]

[0020]  其中K为点集中的点数， R* 和T*为最优旋转矩阵和最优平移矩阵，
                                        pk ,qk分别为两点
集{pk} ,{qk}中第k对点的三维坐标， 解得：



[0021]




[0022]

[0023]   其中                                     协方差矩阵Sc
                               U和V是协方差矩阵Sc的奇异值分解，

＝XYT ，
     其中X ,Y分别由列向量              和             k＝1 ,2 ,...,K；
                                           组成，
[0024]                   计算平均匹配距离误差为：
         求得最优旋转矩阵和最优平移矩阵后，

[0025]

[0026]        基于两点集的平均匹配距离误差，
         S4 .3：             分别计算节点间相似度和连接边间相似
度；
[0027]   两图G ,G '中的两节点Gi ,G 'i '的相似度为：
[0028]

[0029]   其中      为图G中节点Gi的采样点集，        为图G '中节点G 'i '的采样点集；
[0030]   两图G ,G '中的两连接边Gij ,G 'i 'j '的相似度为：
[0031]

[0032]   其中      为图G中连接边Gij的采样点集，           为图G '中连接边G 'i 'j '的采样点集；
[0033]   利用图匹配方法对两模型的顶点进行预匹配，
       S5：                                           其
                                     定义两图G ,G '的匹配矩阵M，
                                  否则为0；
元素mii '在图G的节点Gi和图G '的节点G 'i '匹配时为1，   寻找使总相似度最大的最优


                                   6
CN 116433837 A             说    明       书                   3/8 页

匹配矩阵M* ：

[0034]

[0035] 使用投影不动点迭代法求解M* ，            得到两图的节点匹配结果， 图中匹配的两节点在
模型中对应的面即为预匹配的面，             当两模型中的两边所邻接的面均为预匹配的面时，        这两
条边为预匹配的边，        同样地，   当两模型中的两顶点所邻接的边均为预匹配的边时，      这两个顶
点为预匹配的顶点；
[0036] S6： 通过误匹配消除方法计算模型间刚体变换关系，                      计算预
                                             给定最大允许误差ε，
匹配顶点集的平均匹配距离误差，             若其小于最大允许误差ε，    则此时的最优旋转矩阵和最优
                                 M  M
平移矩阵即为模型间的刚体变换参数R 和T ；                  分别计算每对预匹配顶点的匹配距离误
                                      否则，
差：
[0037] E ({p }，
        k   k  {qk})＝||(R*·pk+T*)‑qk||
[0038] 其中E ({p } ,{q })为点集{p } ,{q }中第k对预匹配顶点的匹配距离误差， pk ,qk为
             k  k    k         k    k
点集{pk} ,{qk}中第k对预匹配顶点的三维坐标；
[0039] 在预匹配顶点集中剔除匹配距离误差最大的顶点对后重新计算其平均匹配距离
误差并与最大允许误差ε进行比对，             直至求出模型间的刚体变换参数RM和TM ；
[0040] S7：匹配顶点、     边控制点和面控制点三类关键点，        将所求得的刚体变换参数RM 和TM
代入两模型的每两个顶点，           计算匹配误差：
[0041]

[0042]   其中      为两模型顶点集   和                       若求得的匹配误
                                    第i ,i '个顶点的三维坐标；
差             则对应的两顶点为匹配的顶点；
     小于最大允许误差ε，
[0043]   将RM和TM代入每对边控制点，
                       并分别计算每对边控制点的匹配误差：
[0044]

[0045]   其中      为两模型边控制点集      和                        w为相应
                                        第i ,i '个边控制点的三维坐标，
       若求得的匹配误差
控制点的权重值；                            则对应的两边控制点为匹配的
                           小于最大允许误差ε，
边控制点；
[0046] 将RM和TM代入每对面控制点，
                     并分别计算每对面控制点的匹配误差：
[0047]

[0048]   其中      为两模型面控制点集          和                        w为相
                                            第i ,i '个面控制点的三维坐标，
        若求得的匹配误差
应控制点的权重值；                               则对应的两面控制点为匹配
                               小于最大允许误差ε，
的面控制点；
[0049] S8：
         逐层判断三维CAD模型差异，对两模型的每两条边，若其包含的顶点和边控制
点均一一匹配，    则这两条边视为相同边，否则视为差异边；对两模型中每两个面，若其包含
的面控制点均一一匹配，    且其组成边均为相同边， 则这两个面视为相同面，否则视为差异
面； 若两模型的所有面均为相同面，   则两模型为相同的模型，否则两模型间存在差异。


                                7
CN 116433837 A          说   明   书                  4/8 页

[0050]       所述S2中，
         进一步地，    边控制点的获取方法具体为：
[0051]                  对边所在的曲线
         将模型的边转化为NURBS形式，            有：


[0052]


[0053]          n为B样条曲线的控制点个数，
         其中u为自变量，                      Ni ,k (u)为第i
                             k为B样条曲线的阶次，
                       wi为第i个控制点对应的权重；
个k阶B样条基函数， 为第i个控制点的三维坐标，             从各边中
提取的除顶点外的所有NURBS控制点作为模型的边控制点，并记录其三维坐标和权重。
[0054] 进一步地，    面控制点的获取方法具体为：
           所述S2中，
[0055]                  对面所在的曲面
         将模型的面转化为NURBS形式，             有：


[0056]


[0057]             n ,m为B样条曲线两个维度的控制点个数，
         其中u ,v为自变量，                   k ,l为B样条曲线两个
     Ni ,k (u)为第i个k阶B样条基函数，
维度的阶次，                    Nj ,l (v)为第j个l阶B样条基函数， 为在位
置(i ,j)处的控制点的三维坐标，wi ,j为在位置(i ,j)处的控制点对应的权重；     从各面中提取
的除顶点外的所有NURBS控制点作为模型的面控制点，        并记录其三维坐标和权重。
[0058] 进一步地，在得到三维CAD模型差异分析结果后，      采用Opencascade平台搭建三维
CAD模型差异分析展示界面，  显示待比对的两个三维CAD模型，      并将其中所有的差异面高亮
显示。
[0059] 本发明的有益效果是，本发明可以在考虑建模误差的情况下，          快速、  准确地识别处
于不同姿态下的三维CAD模型间的差异，     并给出直观的差异分析结果。

附图说明
[0060] 下面结合附图和实施例对本发明进一步说明；
[0061] 图1是本发明方法的流程图。
[0062] 图2是两个待比较的三维CAD模型实例。
[0063] 图3是从三维CAD模型中提取关键点示意图。
[0064] 图4是通过图表示和图匹配对三维CAD模型的顶点、边和面进行预匹配的示意图。
[0065] 图5是基于关键点匹配序列对三维CAD模型的边和面进行逐级比较的示意图。
[0066] 图6是三维CAD模型差异分析实例的比较结果。


具体实施方式
[0067] 下面根据附图和优选实施例详细描述本发明， 本发明的目的和效果将变得更加明
白， 应当理解， 此处所描述的具体实施例仅仅用以解释本发明， 并不用于限定本发明。
[0068] 本发明提出的基于关键点匹配的三维CAD模型差异分析方法，首先将三维CAD模型
转化为关键点表示，   然后引入图表示和图匹配方法建立两模型间顶点的预匹配， 并通过误

                            8
CN 116433837 A                说   明   书                    5/8 页

匹配消除方法得到两模型间的刚体变换参数，     之后利用刚体变换检验建立关键点的匹配关
系， 得到关键点匹配序列后进行逐级比较，    从而识别边、面和实体模型的差异。该方法流程
图如图1所示，    具体包括如下步骤：
[0069] S1：
         将待比较的两个三维CAD模型转化为STP格式；
[0070] S2：
         从三维CAD模型中提取顶点、边控制点、面控制点三类控制点；
[0071] 从模型中提取边的交点作为顶点，  并记录顶点的三维坐标；
[0072]                  对边所在的曲线
         将模型的边转化为NURBS形式，                   有：


[0073]


[0074]          n为B样条曲线的控制点个数，
         其中u为自变量，                      Ni ,k (u)为第i
                             k为B样条曲线的阶次，
                       wi为第i个控制点对应的权重；
个k阶B样条基函数， 为第i个控制点的三维坐标，             从各边中
                           并记录其三维坐标和权重。
提取的除顶点外的所有NURBS控制点作为模型的边控制点，
[0075]                  对面所在的曲面
         将模型的面转化为NURBS形式，                    有：


[0076]


[0077]             n ,m为B样条曲线两个维度的控制点个数，
         其中u ,v为自变量，                   k ,l为B样条曲线两个
     Ni ,k (u)为第i个k阶B样条基函数，
维度的阶次，                    Nj ,l (v)为第j个l阶B样条基函数， 为在位
置(i ,j)处的控制点的三维坐标，   wi ,j为在位置(i ,j)处的控制点对应的权重；从各面中提取
的除顶点外的所有NURBS控制点作为模型的面控制点，           并记录其三维坐标和权重。
[0078] S3： 将三维CAD模型转化为图表达形式以进行顶点预匹配，       模型的每个面对应图的
一个节点，   若模型中的两个面共边，     则这两个面在图中对应的两个节点间存在一个连接边，
对应两面的公共边，     否则两个节点间不存在连接边；
[0079] S4： 计算两图节点间和连接边间的相似度，        包括如下子步骤：
[0080] S4 .1：
            在节点对应的模型面上利用NURBS公式进行均匀采样，      即：

[0081]

[0082]   其中                   n＝1 ,...,N1×N1 ，
                 为第i个节点Gi的采样点集，                    Si (u ,v)为节点
                                             N1为采样数，
Gi对应的模型面Si上利用NURBS公式得到的采样点；
[0083] 在连接边对应的模型边上利用NURBS公式进行均匀采样，
                                 即：

[0084]


[0085]   其中                           m＝1 ,...,N2 ，
                 为第i ,j个节点间连接边Gij的采样点集，                 Cij (u)
                                                  N2为采样数，
为连接边Gij对应的模型边Cij上利用NURBS公式得到的采样点；
[0086] S4 .2：
            基于刚体变换计算两点集的平均匹配距离误差， 对任意点数相同的两点集

                                  9
CN 116433837 A                说    明    书                        6/8 页

         可寻找最优刚体变换：
{pk}和{qk}，

[0087]

[0088]  其中K为点集中的点数， R* 和T*为最优旋转矩阵和最优平移矩阵，
                                        pk ,qk分别为两点
集{pk} ,{qk}中第k对点的三维坐标， 可解得：



[0089]




[0090]

[0091]   其中                                     协方差矩阵Sc
                               U和V是协方差矩阵Sc的奇异值分解，

＝XYT ，
     其中X ,Y分别由列向量              和              k＝1 ,2 ,...,K；
                                            组成，
[0092]                   计算平均匹配距离误差为：
         求得最优旋转矩阵和最优平移矩阵后，

[0093]

[0094]        基于两点集的平均匹配距离误差，
         S4 .3：             分别计算节点间相似度和连接边间相似
度；
[0095]   两图G ,G '中的两节点Gi ,G 'i '的相似度为：
[0096]

[0097]   其中      为图G中节点Gi的采样点集，         为图G '中节点G 'i '的采样点集；
[0098]   两图G ,G '中的两连接边Gij ,G 'i 'j '的相似度为：
[0099]

[0100]   其中      为图G中连接边Gij的采样点集，            为图G '中连接边G 'i 'j '的采样点集；
[0101] S5：利用图匹配方法对两模型的顶点进行预匹配，                       其
                                     定义两图G ,G '的匹配矩阵M，
                                  否则为0；
元素mii '在图G的节点Gi和图G '的节点G 'i '匹配时为1，   寻找使总相似度最大的最优
        *
匹配矩阵M ：

[0102]

[0103] 使用投影不动点迭代法求解M* ，
                      得到两图的节点匹配结果，图中匹配的两节点在
模型中对应的面即为预匹配的面，   当两模型中的两边所邻接的面均为预匹配的面时，  这两
条边为预匹配的边，      当两模型中的两顶点所邻接的边均为预匹配的边时，
            同样地，                       这两个顶
点为预匹配的顶点；
[0104] S6：
         通过误匹配消除方法计算模型间刚体变换关系，          计算预
                               给定最大允许误差ε，


                                   10
CN 116433837 A             说    明     书                    7/8 页

匹配顶点集的平均匹配距离误差，              若其小于最大允许误差ε，     则此时的最优旋转矩阵和最优
                                  M   M
平移矩阵即为模型间的刚体变换参数R 和T ；                    分别计算每对预匹配顶点的匹配距离误
                                        否则，
差：
[0105] E ({p } ,{q })＝||(R*·p +T*)‑q ||
        k   k     k          k      k
[0106] 其中E ({p } ,{q })为点集{p } ,{q }中第k对预匹配顶点的匹配距离误差，   pk ,qk为
             k   k    k         k     k
点集{pk} ,{qk}中第k对预匹配顶点的三维坐标；
[0107] 在预匹配顶点集中剔除匹配距离误差最大的顶点对后重新计算其平均匹配距离
误差并与最大允许误差ε进行比对，              直至求出模型间的刚体变换参数RM和TM ；
[0108] S7：匹配顶点、      边控制点和面控制点三类关键点，         将所求得的刚体变换参数RM 和TM
代入两模型的每两个顶点，            计算匹配误差：
[0109]

[0110]   其中      为两模型顶点集    和                       若求得的匹配误
                                     第i ,i '个顶点的三维坐标；
差             则对应的两顶点为匹配的顶点；
     小于最大允许误差ε，
[0111]   将RM和TM代入每对边控制点，
                       并分别计算每对边控制点的匹配误差：
[0112]

[0113]   其中      为两模型边控制点集      和                       w为控制
                                       第i ,i '个边控制点的三维坐标，
     若求得的匹配误差
点的权重值；                          则对应的两边控制点为匹配的边控
                       小于最大允许误差ε，
制点；
[0114]   将RM和TM代入每对面控制点，
                       并分别计算每对面控制点的匹配误差：
[0115]

[0116]   其中      为两模型面控制点集       和                         w为控
                                          第i ,i '个面控制点的三维坐标，

      若求得的匹配误差
制点的权重值；                             则对应的两面控制点为匹配的面
                           小于最大允许误差ε，
控制点；
[0117] S8：逐层判断三维CAD模型差异，   对两模型的每两条边，  若其包含的顶点和边控制
点均一一匹配，    则这两条边视为相同边，    否则视为差异边；  对两模型中每两个面，若其包含
的面控制点均一一匹配，    且其组成边均为相同边，      则这两个面视为相同面， 否则视为差异
面； 若两模型的所有面均为相同面，    则两模型为相同的模型，    否则两模型间存在差异；
[0118] S9：
         显示匹配结果，                                 显
                 采用Opencascade平台搭建三维CAD模型差异分析展示界面，
示待比对的两个三维CAD模型，    并将其中所有的差异面高亮显示。
[0119] 下面针对具体的实施例对本发明的方法进行描述。
[0120] 选择具体的两个三维CAD模型进行差异分析，      如图2所示，待比较的两个模型为具
有开孔和开槽的毛坯件，    其中模型A开槽形状为梯形槽，     模型B开槽形状为矩形槽，除此之外
两模型间没有其他差异；
[0121] S1：
         将待比较的两个三维CAD模型转化为STP格式；
[0122] S2：
         分别从两模型中提取关键点，     图3为模型A的关键点示意图， 两个模型都由两个
圆柱面和11个平面组成，   都有22个顶点，  22个边控制点和22个面控制点；

                                11
CN 116433837 A               说   明     书                         8/8 页

[0123]   将三维CAD模型转化为图表达形式，
       S3：                          如图4所示，   图的每一个顶点对应原模型中
的一个面，   顶点间的每一个连接对应原模型中两面的公共边；
[0124] S4：
         计算两图节点间和连接边间的相似度；
[0125] S5：
         使用投影不动点迭代法(IPFP，       integer projected fixed point method)得
到图匹配结果，    将图匹配结果对应回原CAD模型，         如图4所示，   得到预匹配的面、       边和顶点；
[0126] S6：通过误匹配消除方法计算模型间刚体变换关系，                给定最大允许误差ε，        计算预
匹配的顶点集的平均匹配距离误差大于最大允许误差，                  因此剔除匹配距离误差后再次计算
迭 代 ，直 到 平 均 匹 配 距 离 误 差 小 于 最 大 允 许 误 差 ，并 求 出 模 型 间 的 刚 体 变 换 参 数




[0127] S7：
         匹配顶点、  边控制点和面控制点， 将求得的刚体变换参数RM和TM代入每对顶点、
边控制点和面控制点，     计算其匹配误差， 共匹配14个顶点、22对边控制点和22对面控制点；
[0128] S8：
         对两三维CAD模型逐级比较，  图5为模型A的组成层级图， 在比较两模型的边时，
若其包含的全部顶点和边控制点均一一匹配，        则这两条边视为相同边， 否则视为差异边；两
模型共有17条相同边，    10条差异边；在比较两模型的面时，  若其包含的全部边均为相同边，
且其包含的全部面控制点均一一匹配，       则这两个面视为相同面，  否则视为差异面；两模型共
有8个相同面，    5个差异面；由于两模型存在差异面，  因此两模型为有差异的模型；
[0129] S9：将比对结果输出，  如图6所示，在软件界面中展示两三维CAD模型， 并高亮展示
具有差异的面；
[0130] 本领域普通技术人员可以理解，    以上所述仅为发明的优选实例而已 ，  并不用于限
制发明，   尽管参照前述实例对发明进行了详细的说明，     对于本领域的技术人员来说， 其依然
可以对前述各实例记载的技术方案进行修改，        或者对其中部分技术特征进行等同替换。凡
在发明的精神和原则之内，      所做的修改、等同替换等均应包含在发明的保护范围之内。




                                  12
CN 116433837 A   说   明   书    附   图   1/3 页




                         图1




                         13
CN 116433837 A   说   明   书    附   图   2/3 页




                         图2




                         图3




                         14
CN 116433837 A   说   明   书    附   图   3/3 页




                         图4




                         图5




                         图6



                         15
